<ripple
    xmlns:android="http://schemas.android.com/apk/res/android"
    android:color="@color/colorAccent">

    <item android:id="@android:id/mask">
        <shape android:shape="rectangle">
            <solid android:color="@color/colorPrimaryDark" />
            <corners android:radius="@dimen/padding_reward_system" />
        </shape>
    </item>

    <item android:id="@android:id/background">
        <shape android:shape="rectangle"
            xmlns:android="http://schemas.android.com/apk/res/android">
        <gradient
            android:startColor="@color/colorAccent"
            android:centerColor="@color/purple" android:angle="135"
            android:endColor="@color/orange">
        </gradient>
        <corners android:topLeftRadius="@dimen/padding_reward_system" android:bottomRightRadius="@dimen/padding_reward_system"
            android:bottomLeftRadius="@dimen/padding_reward_system" android:topRightRadius="@dimen/padding_reward_system"/>
        </shape>
    </item>
</ripple>