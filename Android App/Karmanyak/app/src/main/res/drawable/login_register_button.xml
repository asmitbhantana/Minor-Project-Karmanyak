
<ripple
xmlns:android="http://schemas.android.com/apk/res/android"
android:color="@color/colorAccent">

<item android:id="@android:id/mask">
    <shape android:shape="rectangle">
        <solid android:color="@color/colorPrimaryDark" />
        <corners android:radius="@dimen/padding_reward_system" />
    </shape>
</item>

<item android:id="@android:id/background">
    <shape android:shape="rectangle"
        xmlns:android="http://schemas.android.com/apk/res/android">
        <corners android:radius="@dimen/button_round"/>
        <gradient android:angle="45" android:startColor="@color/primaryDarkColor" android:centerColor="@color/primaryLightColor" android:endColor="@color/secondaryColor"/>
    </shape>
</item>
</ripple>